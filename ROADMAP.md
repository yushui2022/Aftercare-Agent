# 实现路线图

当前状态：参考架构与可执行 EGM 证据适配层；下列业务阶段尚未整体验收。阶段表示依赖顺序，不代表发布日期或生产能力承诺。

已完成：历史源码评估、0.5 回执契约修复，以及 0.6 嵌入式应用层和 PostgreSQL 存储。Aftercare 的受限证据 Adapter 已有合成回执测试；真实业务运行时仍未实现。EGM 并发存储通过真实数据库测试，不等于 Aftercare 已有任务调度或租约。见[最新接入决策](docs/decisions/0002-embedded-egm.md)与[接入验收](docs/integrations/egm-embedded.md)。

## 1. 离线业务验证

目标：验证 Agent 对“未收到货”售后调查是否有实际帮助。

交付范围：

- 合成或获准脱敏的案件样本与评测集。
- 订单、物流、附件与通知工具的模拟接口。
- 有限工具集合下的单任务执行循环。
- 证据引用、错误动作、人工接管与成本评测。
- 固定已审核的 EGM 0.6 源码提交，将现有嵌入式 Evidence Adapter 接入调查循环；按租户/Case 隔离，保持默认关闭长期记忆注入。
- 将 EGM 与简单显式证据规则作对照，区分准入价值、语义校验和上下文成本。

验收：能够重复运行评测，明确成功和失败口径；失败回执、另一订单回执和越界记忆必须在 Aftercare 接入边界被阻止。来源、时间、TTL 和高风险节点类型不能由模型自由赋值。不接入真实退款或补发权限。

## 2. 单 Worker 持久执行

目标：执行不再依赖一个 HTTP 连接或单次进程存活。

交付范围：

- Case、Run、Step、Attempt 与检查点。
- 持久等待条件、外部事件 Inbox 与定时唤醒。
- Action Ledger、审批绑定、幂等键与 UNKNOWN 处理。
- 事务性事件与 Outbox。
- PostgreSQL 业务权威与 EGM 解释投影分离；持久化来源事件、策略版本、导入进度及对象映射。
- 导入重放保留原始观察时间；证据失效触发复核，不重做已经确认的动作。

验收：重启后任务可恢复；回执缺失不触发盲目重复操作；回复早于等待登记到达时不漏唤醒。EGM 导入中断或投影滞后时，高风险路径暂停或转人工，而不是依据旧的 accepted 结果继续执行。

## 3. 多 Worker 与多租户并发

目标：执行权、资源准入和业务写入都有明确边界。

交付范围：

- 任务租约、续租、fencing 与状态版本控制。
- 租户配额、分资源限流、公平调度与背压。
- 重投去重、同订单竞争和跨实例额度管理。
- 运行指标、追踪及独立业务审计。
- 将已验证的 EGM PostgreSQL 幂等/revision 协议接入真实 Worker；验证投影接管、业务租约 fencing 与跨工单同订单竞争。仅可信宿主持有数据库权限，模型与沙箱不获得连接或裸存储 API。

验收：注入旧 Worker 恢复、消息重投、供应商限流等故障，验证状态与副作用处理。

## 4. 按需沙箱

目标：高风险工具进入隔离环境，控制状态不依赖沙箱存活。

交付范围：

- Sandbox Adapter 与一个经过验证的后端。
- 出网、文件、资源及短期凭证限制。
- 产物保存、环境回收、过期清理与重建。
- 与业务状态及工具权限网关的关联。

验收：租户数据隔离有效；沙箱失联或 OOM 不误报业务成功；重建不会重复已确认动作。

## 5. 容量与延迟优化

目标：根据测量结果优化，而不是以更多组件替代容量分析。

交付范围：

- 稳态、突发与集中唤醒负载测试。
- 暖池、补池速率、分层伸缩与成本测量。
- 模型与第三方 API 的限流、重试预算和过载策略。

验收：公开测试条件、数据规模及限制，分别报告队列、模型、工具、沙箱与人工等待时间。

## 6. 按实际需要扩展

以下属于候选能力，不默认全部实施：

- 独立持久消息系统与多下游事件回放。
- ACP 兼容客户端适配。
- 经过权限约束、来源可追溯的长期记忆；先验证 EGM 的候选记忆是否足够，再决定是否需要 Letta 或 TencentDB Agent Memory。
- 更多模型供应商与检查点级故障切换。

采用条件：能解释它解决了什么已测量的问题，如何与现有状态所有权配合，以及新增的运维成本。

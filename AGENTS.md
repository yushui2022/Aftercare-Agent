# Aftercare Agent：执行与接手规则

本文件是本仓库的恢复入口，不是自动执行授权。遵守当前用户请求和更高优先级指令；用户只要求评估、阅读或规划时，不因任务表里有 READY 项就开始实现、部署、提交或推送。

## 新会话、上下文压缩后或接手时

1. 阅读 [当前状态](docs/project-status.md)：当前任务、代码基线、验证证据、未提交变更和下一步。
2. 阅读 [工程执行计划](docs/engineering-plan.md) 中对应任务的目标、依赖、文件与验收；首次接手读完整计划。
3. 阅读 [技术栈](docs/tech-stack.md)，以及当前模块需要的 [工程总设计](docs/system-design.md) 章节。触及 EGM 时还要读 [ADR-0002](docs/decisions/0002-embedded-egm.md) 和 [嵌入式接入](docs/integrations/egm-embedded.md)。
4. 只读核对当前目录、Git HEAD、工作区差异和相关源码。状态文档只是上次观察，不得覆盖现在的代码事实；出现差异先解释和核对，不擅自回滚。
5. 向用户简述本轮目标、将改的模块、验证方式与风险，再执行本轮获准范围内的任务。

这里的“记忆”是开发过程的持久接手记录，与产品中的 EGM 证据记忆、客户 Session 和运行检查点是不同层次。

## 文档职责和冲突处理

- docs/project-status.md：唯一可变进度台账；任务状态、最近验证、当前接手点只在此维护。
- docs/engineering-plan.md：任务 ID、依赖、交付和完成条件，不维护第二套完成勾选框。
- docs/tech-stack.md：语言、运行时、依赖策略与技术选择；精确安装版本最终由锁文件和镜像记录确定。
- docs/system-design.md：系统行为、不变量与目标架构；不是现有功能清单。
- ROADMAP.md：阶段导航，与执行计划使用同一组阶段名称。
- docs/decisions/：持久架构决策；新 ADR 必须明确替代哪项旧决策，不能按文件日期猜优先级。

代码/测试证明实际行为，不自动证明行为符合设计。代码与决策冲突时记录差异；先确认是待实现、缺陷还是获准的设计变更。不要把不正确实现反向写成新规范。

## 不得退回的架构边界

- EGM 独立仓库/Python 包，默认嵌入可信 Aftercare Worker；HTTP 可选，PostgreSQL 共享，SQLite 仅本地/组件验证。
- 普通嵌入不自动共享业务事务；只有显式 join 同连接外层事务才一起提交，提交前结果是暂定的。
- Run 执行权、EGM revision、Case 业务版本、Action 幂等键、SSE 游标互不替代。
- Run 的权威租约/fencing 在 Run 范围；网络调用不能放进长数据库事务。
- 等待与恢复必须持久化；等待满足、消费应用记录和唯一唤醒同事务提交。
- 当前 evidence.py 只覆盖退款证据；订单/物流调查需要自己的契约，不能冒用 refund_completed。
- 阶段 A 只生成建议和通知草稿，使用模拟业务连接器，不真实退款、补发或发邮件。
- 模型与沙箱不得持有数据库连接、裸 EGM 对象、高权限 Principal 或供应商主密钥。
- 沙箱默认按需调用；Kafka、NATS、Redis、ACP、长期记忆平台不是必须同时安装的依赖。

## 工作纪律

- 开始代码任务前，在状态台账记录任务 ID、执行者/会话标识（已知时）、涉及文件及验收。多个 Agent 不同时修改同一模块；共享契约先由一个负责者固定。
- 一个切片完成后，先跑对应验证，再更新状态台账。DONE 必须有可定位的产物和验证证据；写完设计不等于实现完成。
- 在阶段切换、可控的会话结束/交接前保存检查点：改了什么、没有做什么、下一步、风险、实际命令与结果、Git 状态。不能保证突然中断前一定保存，恢复仍需核对 diff。
- 未运行写“未运行”；失败写真实失败；历史测试数量必须附对应版本，不能冒充当前结果。
- 提交、推送、部署、真实业务动作分别记录，不互相推断。仅在当前请求授权时执行，不自动沿用旧会话的授权。
- 只读评估不需要为了“更新状态”而修改文件，直接报告发现即可。
- 不保存密钥、客户数据、完整聊天转录或私有推理。记录决策及简洁理由、结果与证据引用。

## 测试与文档预算

产品是主线，测试和文档是护栏。历史账：测试/产品行数比 0.78:1，文档/产品 0.46:1，其中还有 13 个只记录 CI 结果的提交。那部分时间不是开发。以下按硬预算执行：

- 一个切片新增的测试行数不超过新增产品行数的一半。切片收尾时若超了，先删测试，再继续写功能。
- 只为不变量写用例：状态机合法边、幂等键、租约与 fencing、事务原子性、并发与恢复、跨租户隔离、外部副作用不重放。这些写错就是生产事故。
- 不为这些写用例：错误消息措辞（`match=` 只用来区分“拒绝了”和“没拒绝”，不复述文案）、参数解析的每个分支、配置默认值、纯格式或顺序拼装、同一逻辑的第二种等价输入，以及契约测试已覆盖的行为在集成层再验一遍。
- 参数化只用于输入形状确实不同、失败时能指出是哪种形状的场景，不用来把同一个断言铺成五个用例。
- 真实 PostgreSQL 集成测试在切片末尾跑一次全量，不在调试循环里反复跑。缺 DATABASE_URL 时跳过就是跳过，跳过不等于验收。
- 跨文件共用的 fixture 只保留一份（PostgreSQL 三件套在 tests/conftest.py）；不要在每个测试文件里复制同一段 setup。
- 每个切片只更新一处文档：docs/project-status.md 台账。ADR 只在真的做出了架构决策时新增；development/operations/contracts/api 等手册按版本批量对齐，不逐切片改。
- 不提交“只记录 CI 结果”的独立提交，也不为台账提交本身再补一次 CI 记录；CI 结论并入下一次真实改动。
- 计划与总结写目标、涉及文件、执行顺序、验证命令和真实结果，不抄生产代码或用例正文。

## 文件、缓存与验证

- 本机仓库默认 G:\Projects\Aftercare-Agent，EGM 在 G:\Projects\Evidence-Gated-Memory；脚本不得把个人绝对路径写死为运行前提。
- 搜索优先 rg；本机可用 C:\Users\xiaoy\.local\bin\rg.exe。文本编辑用 apply_patch，保留无关未提交变更。
- 依赖/构建跟随 G 盘项目；uv/pip/npm/pnpm 缓存分别使用 G:\DevCache 下对应目录；临时验证使用 G:\DevCache\Temp\aftercare-*。大型操作先查空间，Docker/WSL 磁盘另行核验，不擅自迁移。
- 当前可执行命令、已具备工具和未来命令必须区分。没有 pyproject/锁文件/服务入口时，不能声称项目已可 uv sync 或启动完整服务。
- 每个实现切片包含相关单测、必要的真实 PostgreSQL 集成/故障测试和文档检查；不得用 SQLite 测试替代 PostgreSQL 租约与并发验收。数量上限见上一节。
